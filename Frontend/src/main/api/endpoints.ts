/**
    Replace with .env in future if possible
 */
enum Endpoints {
    SHORTENER_URL = "http://localhost/api/links",
    API_URL = "http://localhost",
    PATH_LOGIN = "login",
    PATH_LINKS = "api/links",
    PATH_USERS = "api/user/users",
    PATH_SCHEDULER = "api/scheduler"
}

export default Endpoints;